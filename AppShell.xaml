<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:ProiectMediiMobile1.Pages" 
  x:Class="ProiectMediiMobile1.AppShell">

    <FlyoutItem Title="Home">
        <ShellContent ContentTemplate="{DataTemplate pages:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Salon">
        <!-- Adjust reference if SalonMainPage is in a sub-namespace -->
        <ShellContent ContentTemplate="{DataTemplate pages:SalonMainPage}" />
    </FlyoutItem>

    <!-- Add other text-based menu items as needed -->
</Shell>
